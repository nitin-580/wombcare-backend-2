version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:8080"
    environment:
      - NODE_ENV=development
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
